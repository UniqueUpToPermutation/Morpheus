#pragma once

#include <Engine/Resources/Resource.hpp>

namespace Morpheus {
	typedef RefHandle<TextureResource> TextureComponent;
	typedef RefHandle<PipelineResource> PipelineComponent;
	typedef RefHandle<MaterialResource> MaterialComponent;
	typedef RefHandle<GeometryResource> GeometryComponent;
}